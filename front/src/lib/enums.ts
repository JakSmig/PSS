export enum Paths {
  Home = '/',
  Capital = '/capital/:capital',
  Map = '/map',
  Profile = '/profile',
  Admin = '/admin',
}

export enum SignInVariant {
  SignIn = 'sign_in',
  SignUp = 'sign_up',
  Reset = 'reset',
}
