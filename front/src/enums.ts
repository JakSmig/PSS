export enum Paths {
  Home = '/',
  Capital = '/capital/:capital',
  Map = '/map',
  Profile = '/profile',
}

export enum SignInVariant {
  SignIn = 'sign_in',
  SignUp = 'sign_up',
  Reset = 'reset',
}
